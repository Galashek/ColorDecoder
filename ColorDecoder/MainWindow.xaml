<Window x:Class="ColorDecoder.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ColorDecoder"
        mc:Ignorable="d"
        Title="Guess Colors" Height="600" Width="550" ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid x:Name="GamePanel" Grid.Row="0" Background="Beige">
            <Button Width="50" Height="30" HorizontalAlignment="Left" Margin="10" Click="StartGame">Restart</Button>
            <Button x:Name="CheckButton" Width="50" Height="30" HorizontalAlignment="Right" Margin="10" Click="Check">Check</Button>
        </Grid>
        <Grid x:Name="GameField" ShowGridLines="True" Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
        </Grid>
    </Grid>
</Window>
